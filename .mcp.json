{
  "mcpServers": {
    "vimeo": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_DATA}/vimeo-mcp/index.js"],
      "env": {
        "VIMEO_ACCESS_TOKEN": "${user_config.vimeo_access_token}"
      }
    }
  }
}
