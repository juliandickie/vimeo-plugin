# Everything under docs/ stays local and untracked by request.
# This includes the cloned Vimeo reference repos (vimeo.js/php/py/openapi),
# the design specs, and any research notes.
docs/

# Node deps for the bundled MCP server are installed into the plugin data dir,
# never committed.
node_modules/
servers/vimeo-mcp/node_modules/

# OS cruft
.DS_Store
