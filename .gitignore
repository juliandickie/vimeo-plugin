# Cloned Vimeo reference repos - studied, not part of this plugin's source.
# Each carries its own .git; excluded to keep history clean and avoid nested repos.
docs/vimeo.js/
docs/vimeo.php/
docs/vimeo.py/
docs/openapi/

# Node deps for the bundled MCP server are installed into the plugin data dir,
# never committed.
node_modules/
servers/vimeo-mcp/node_modules/

# OS cruft
.DS_Store
